const BlockCenterMedium = ({children, background}) => {
    return(
        <div className= {`block-center-medium ${background}`}>
            {children}
        </div>
    )
}

export default BlockCenterMedium